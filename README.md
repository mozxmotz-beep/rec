## 6. 已工程化的优先级特征改造

- P0：
  - 多窗口统计（1/3/7/14/30/90 天）；
  - Beta 平滑比率（用于 CTR/CVR 类平滑先验）；
  - 漂移特征（3 天 vs 30 天）；
  - 多 half-life 衰减（hl3 / hl14）；
  - 防泄漏时序重构：全部基于样本时间戳回看历史序列桶构造，不读取未来行为。
- P1：
  - session/recency（最小时间间隔倒数）；
  - 序列熵 proxy（行为分布熵）。
- P2：
  - 已新增 ESMM/MMOE 训练模式（`--task_type esmm_mmoe`）；
  - 已新增点时间特征存储原型 `point_time_store.py`（支持在线增量更新与衰减快照）；
  - 下一步建议：接入持久化 KV / 流式消费链路用于线上服务化。
